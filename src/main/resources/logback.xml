<configuration>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <charset>UTF-8</charset>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{35} - %msg %exception [EOL] %n</pattern>
        </encoder>
    </appender>

    <appender name="KAFKA" class="com.github.danielwegener.logback.kafka.KafkaAppender">
        <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
            <providers>
                <pattern>
                    <pattern>
                        {
                        "date": "%d{yyyy-MM-dd HH:mm:ss}",
                        "milliseconds": "%d{SSS}",
                        "host": "${HOSTNAME}",
                        "thread": "%thread",
                        "level": "%-5level",
                        "logger": "%logger{60}",
                        "message": "%msg",
                        "exception": "%exception"
                        }
                    </pattern>
                </pattern>
            </providers>
        </encoder>
        <topic>porter-service</topic>
        <keyingStrategy class="com.github.danielwegener.logback.kafka.keying.HostNameKeyingStrategy" />
        <deliveryStrategy class="com.github.danielwegener.logback.kafka.delivery.AsynchronousDeliveryStrategy">
        </deliveryStrategy>
        <producerConfig>bootstrap.servers=${LOGS_KAFKA}</producerConfig>
        <producerConfig>client.id=PORTER-${HOSTNAME}</producerConfig>
        <producerConfig>compression.type=gzip</producerConfig>
        <producerConfig>retries=2</producerConfig>
        <producerConfig>acks=1</producerConfig>
        <producerConfig>linger.ms=10000</producerConfig>
    </appender>

    <logger name="ru.stdev.egts" level="DEBUG"/>
    <root level="WARN">
        <appender-ref ref="STDOUT"/>
        <appender-ref ref="KAFKA" />
    </root>
</configuration>